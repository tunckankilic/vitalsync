/// VitalSync — Glassmorphic Card Widget (barrel export).
///
/// Re-exports the canonical implementation from the fitness widgets folder.
/// This file exists for backward compatibility — all imports pointing here
/// will transparently resolve to the feature-rich version.
library;

export 'fitness/glassmorphic_card.dart';
